apiVersion: batch/v1
kind: Job
metadata:
  name: {{ include "nginx.fullname" . }}-create
  labels:
    app.kubernetes.io/component: admission-webhook
    app.kubernetes.io/part-of: ingress-nginx
  {{- include "nginx.labels" . | nindent 4 }}
spec:
  template:
    metadata:
      labels:
        app.kubernetes.io/component: admission-webhook
        app.kubernetes.io/instance: ingress-nginx
        app.kubernetes.io/name: ingress-nginx
        app.kubernetes.io/part-of: ingress-nginx
        app.kubernetes.io/version: 1.7.0
      name: ingress-nginx-admission-create
    spec:
      containers:
      - args: {{- toYaml .Values.create.create.args | nindent 8 }}
        env:
        - name: POD_NAMESPACE
          valueFrom:
            fieldRef:
              fieldPath: metadata.namespace
        - name: KUBERNETES_CLUSTER_DOMAIN
          value: {{ quote .Values.kubernetesClusterDomain }}
        image: {{ .Values.create.create.image.repository }}:{{ .Values.create.create.image.tag
          | default .Chart.AppVersion }}
        imagePullPolicy: {{ .Values.create.create.imagePullPolicy }}
        name: create
        resources: {}
        securityContext: {{- toYaml .Values.create.create.containerSecurityContext
          | nindent 10 }}
      nodeSelector: {{- toYaml .Values.create.nodeSelector | nindent 8 }}
      restartPolicy: OnFailure
      securityContext:
        fsGroup: 2000
        runAsNonRoot: true
        runAsUser: 2000
      serviceAccountName: ingress-nginx-admission